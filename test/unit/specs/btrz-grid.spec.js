import Vue from 'vue'
import BtrzGrid from '../../../src/btrz-grid.vue'
import { expect } from "chai";

describe('BtrzGrid', () => {
  it('The component name should be BtrzGrid', () => {
    expect(BtrzGrid.name).to.equal('BtrzGrid')
  })
})
