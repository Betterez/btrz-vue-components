import "./setup";

import {expect} from "chai";
import {mount} from "avoriaz";
import BtrzAlert from "../src/btrz-alert";

describe("BtrzAlert", () => {
  let wrapper = null;

  beforeEach(() => {
    wrapper = mount(BtrzAlert);
  })

  // TODO: Add testing
  it("should mount the modal without failing", () => {

  });
})
